class facility {
  dynamic Clinic_Name, maximum, Address, Time, waiting_max;

//  dynamic waiting_list, waiting_time;

  facility(this.Clinic_Name, this.maximum, this.Address, this.Time,
      this.waiting_max);
}
